#include<iostream>
using namespace std;
int main(int argc, char**argv){
    int n;
    cin>>n;
    
    // the bulb will glow have odd number of factor but the bulb have factor even will be off so we have to prints all the perfect squre because they have 2*n+1 factor.
    
    for(int i=1;i*i<=n;i++){
        cout<<i*i<<endl;
    }
    return 0;
}
